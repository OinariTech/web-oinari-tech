name: Deploy to Cloud Run

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

env:
  REGION: asia-northeast1
  SERVICE: web-oinari-tech
  REPOSITORY: web-oinari-tech
  NEXT_PUBLIC_SITE_URL: https://oinaritech.com
  NEXT_PUBLIC_GA_MEASUREMENT_ID: G-W8DJZR2WFM

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build image
        run: |
          IMAGE="${{ env.REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE }}:${{ github.sha }}"
          docker build \
            --build-arg NEXT_PUBLIC_SITE_URL="${{ env.NEXT_PUBLIC_SITE_URL }}" \
            --build-arg NEXT_PUBLIC_GA_MEASUREMENT_ID="${{ env.NEXT_PUBLIC_GA_MEASUREMENT_ID }}" \
            -t "$IMAGE" .
          echo "IMAGE=$IMAGE" >> "$GITHUB_ENV"

      - name: Push image
        run: docker push "$IMAGE"

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ env.SERVICE }}
          region: ${{ env.REGION }}
          image: ${{ env.IMAGE }}
          # Runtime-only secrets for /admin (Google login + session signing).
          # Not build args: these must not end up in the image or client bundle.
          secrets: |
            GOOGLE_OAUTH_CLIENT_ID=GOOGLE_OAUTH_CLIENT_ID:latest
            GOOGLE_OAUTH_CLIENT_SECRET=GOOGLE_OAUTH_CLIENT_SECRET:latest
            SESSION_SECRET=SESSION_SECRET:latest
            ADMIN_PATH=ADMIN_PATH:latest
